# Core deps
streamlit>=1.32
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-chess>=1.999
Pillow>=10.2
anthropic>=0.40
pydantic>=2.6

# Vision deps. Pin numpy<2 and opencv<4.12 so they coexist with TF 2.15 below.
numpy>=1.26,<2.0
opencv-python-headless>=4.9,<4.12

# Offline neural classifier (board_to_fen). tensorflow-cpu instead of
# tensorflow keeps the install small enough for Streamlit Community Cloud
# (~150 MB vs ~600 MB) without losing inference quality — there's no GPU on
# Streamlit Cloud anyway. board_to_fen ships a Keras 2 SavedModel, so keras
# is pinned to a matching 2.x release.
board_to_fen>=0.1.1
tensorflow-cpu==2.15.*
keras==2.15.*
