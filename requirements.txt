# Core deps
streamlit>=1.32
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-chess>=1.999
Pillow>=10.2
anthropic>=0.40
pydantic>=2.6
numpy>=1.26
opencv-python-headless>=4.9

# Offline neural classifier (board_to_fen + TensorFlow CPU).
# - tensorflow-cpu (no GPU bits, ~150 MB) keeps install size manageable on
#   Streamlit Community Cloud and other resource-constrained hosts.
# - tf-keras is the Keras 2 backport. board_to_fen ships a Keras 2
#   SavedModel that Keras 3 cannot load; vision_neural.py monkey-patches
#   `sys.modules["keras"]` to tf_keras before board_to_fen imports, so
#   we can use the latest TF (which only ships Keras 3 by default) while
#   still loading the legacy model.
board_to_fen>=0.1.1
tensorflow-cpu>=2.18
tf-keras>=2.18
