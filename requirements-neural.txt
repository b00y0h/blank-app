# The neural classifier is now part of the base install — see
# requirements.txt. This file is kept for backwards compatibility so
# existing `pip install -r requirements-neural.txt` invocations still work,
# but it just re-exports the base requirements.
-r requirements.txt
