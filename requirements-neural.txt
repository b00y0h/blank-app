# The neural classifier (board_to_fen + tensorflow-cpu + tf-keras) is now
# part of the base install — see requirements.txt. This file remains so any
# existing `pip install -r requirements-neural.txt` invocations keep
# working; it just re-exports the base requirements.
-r requirements.txt
